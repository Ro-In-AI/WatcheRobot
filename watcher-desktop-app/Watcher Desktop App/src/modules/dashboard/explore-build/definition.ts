import type { DashboardQuickActionDefinition } from "@/modules/dashboard/shared/types";

export const buildQuickAction: DashboardQuickActionDefinition = {
  id: "build",
  title: "Explore Build",
  caption: "or build your own",
};
